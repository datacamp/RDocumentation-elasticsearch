elasticsearchjdbc:
  build: ./importer
  hostname: elasticsearchjdbc
  volumes:
    - ./importer/data:/opt/elasticsearch-jdbc/data
    - ./importer/logs:/opt/elasticsearch-jdbc/logs
  external_links:
    - esrdocs_es_1:es